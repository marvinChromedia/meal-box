# Base URL the frontend sends API requests to (no trailing slash).
VITE_API_BASE_URL=http://localhost:4000/api

# "mock" serves feature data from in-memory typed fixtures (default — backend
# endpoints aren't deployed yet). "http" sends real requests to VITE_API_BASE_URL.
VITE_API_MODE=mock
