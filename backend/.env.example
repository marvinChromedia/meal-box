PORT=4000
DATABASE_URL=postgres://localhost:5432/mealbox_dev
# Separate database for integration tests — they truncate and reseed it, so this
# must never point at DATABASE_URL or you will lose local dev data.
TEST_DATABASE_URL=postgres://localhost:5432/mealbox_test
CORS_ORIGIN=http://localhost:5173
